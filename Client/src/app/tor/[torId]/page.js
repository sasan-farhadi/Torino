import TorDetails from '@/components/template/TorDetails'
import React from 'react'

const TorId = ({ params }) => {
    return (
        <>
            <TorDetails />
        </>
    )
}

export default TorId
